# tcpdump

StaLiBs currently builds tcpdump from the pinned submodules in:

- `upstream/tcpdump`
- `upstream/libpcap`

Release tags in this repository should match official upstream tcpdump stable release tags, for example `tcpdump-4.99.6`.

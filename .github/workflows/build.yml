name: Build static binaries

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - '*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-tcpdump:
    name: tcpdump (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out tcpdump submodules
        run: git submodule update --init --recursive upstream/tcpdump upstream/libpcap

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build tcpdump
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}'

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata
          cp 'dist/bin/tcpdump-linux-${{ matrix.arch }}' artifact-root/bin/tcpdump
          cp 'dist/metadata/tcpdump-linux-${{ matrix.arch }}.buildinfo.txt' artifact-root/metadata/tcpdump.buildinfo.txt

      - name: Upload tcpdump artifact
        uses: actions/upload-artifact@v7
        with:
          name: tcpdump-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  build-strace:
    name: strace (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out strace submodule
        run: git submodule update --init --recursive upstream/strace

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build strace
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}' scripts/build-strace-alpine.sh

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata
          cp 'dist/bin/strace-linux-${{ matrix.arch }}' artifact-root/bin/strace
          cp 'dist/metadata/strace-linux-${{ matrix.arch }}.buildinfo.txt' artifact-root/metadata/strace.buildinfo.txt

      - name: Upload strace artifact
        uses: actions/upload-artifact@v7
        with:
          name: strace-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  build-gdb:
    name: gdb (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out gdb submodule
        run: git submodule update --init --recursive upstream/gdb

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build gdb
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}' scripts/build-gdb-alpine.sh

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata
          cp 'dist/bin/gdb-linux-${{ matrix.arch }}' artifact-root/bin/gdb
          cp 'dist/metadata/gdb-linux-${{ matrix.arch }}.buildinfo.txt' artifact-root/metadata/gdb.buildinfo.txt

      - name: Upload gdb artifact
        uses: actions/upload-artifact@v7
        with:
          name: gdb-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  build-ripgrep:
    name: ripgrep (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out ripgrep submodule
        run: git submodule update --init --recursive upstream/ripgrep

      - name: Fetch ripgrep tags
        run: git -C upstream/ripgrep fetch --tags --force origin

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build ripgrep
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}' scripts/build-ripgrep-alpine.sh

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata
          cp 'dist/bin/rg-linux-${{ matrix.arch }}' artifact-root/bin/rg
          cp 'dist/metadata/rg-linux-${{ matrix.arch }}.buildinfo.txt' artifact-root/metadata/rg.buildinfo.txt

      - name: Upload ripgrep artifact
        uses: actions/upload-artifact@v7
        with:
          name: ripgrep-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  build-tshark:
    name: tshark (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out Wireshark submodule
        run: git submodule update --init --recursive upstream/wireshark

      - name: Fetch Wireshark tags
        run: git -C upstream/wireshark fetch --tags --force origin

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build tshark
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}' scripts/build-tshark-alpine.sh

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata
          cp 'dist/bin/tshark-linux-${{ matrix.arch }}' artifact-root/bin/tshark
          cp 'dist/metadata/tshark-linux-${{ matrix.arch }}.buildinfo.txt' artifact-root/metadata/tshark.buildinfo.txt

      - name: Upload tshark artifact
        uses: actions/upload-artifact@v7
        with:
          name: tshark-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  build-iproute2:
    name: iproute2 (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out iproute2 submodule
        run: git submodule update --init --recursive upstream/iproute2

      - name: Fetch iproute2 tags
        run: git -C upstream/iproute2 fetch --tags --force origin

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build iproute2 tools
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}' scripts/build-iproute2-alpine.sh

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata
          for tool in ip ss bridge tc; do
            cp "dist/bin/${tool}-linux-${{ matrix.arch }}" "artifact-root/bin/$tool"
            cp "dist/metadata/${tool}-linux-${{ matrix.arch }}.buildinfo.txt" "artifact-root/metadata/$tool.buildinfo.txt"
          done

      - name: Upload iproute2 artifact
        uses: actions/upload-artifact@v7
        with:
          name: iproute2-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  build-lsof:
    name: lsof (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out lsof submodule
        run: git submodule update --init --recursive upstream/lsof

      - name: Fetch lsof tags
        run: git -C upstream/lsof fetch --tags --force origin

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build lsof
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}' scripts/build-lsof-alpine.sh

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata
          cp 'dist/bin/lsof-linux-${{ matrix.arch }}' artifact-root/bin/lsof
          cp 'dist/metadata/lsof-linux-${{ matrix.arch }}.buildinfo.txt' artifact-root/metadata/lsof.buildinfo.txt

      - name: Upload lsof artifact
        uses: actions/upload-artifact@v7
        with:
          name: lsof-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  build-mtr:
    name: mtr (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out mtr submodule
        run: git submodule update --init --recursive upstream/mtr

      - name: Fetch mtr tags
        run: git -C upstream/mtr fetch --tags --force origin

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build mtr
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}' scripts/build-mtr-alpine.sh

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata
          cp 'dist/bin/mtr-linux-${{ matrix.arch }}' artifact-root/bin/mtr
          cp 'dist/metadata/mtr-linux-${{ matrix.arch }}.buildinfo.txt' artifact-root/metadata/mtr.buildinfo.txt

      - name: Upload mtr artifact
        uses: actions/upload-artifact@v7
        with:
          name: mtr-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  build-dig:
    name: dig (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out BIND and LMDB submodules
        run: git submodule update --init --recursive upstream/bind9 upstream/lmdb

      - name: Fetch BIND and LMDB tags
        run: |
          git -C upstream/bind9 fetch --tags --force origin
          git -C upstream/lmdb fetch --tags --force origin

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build dig
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}' scripts/build-dig-alpine.sh

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata
          cp 'dist/bin/dig-linux-${{ matrix.arch }}' artifact-root/bin/dig
          cp 'dist/metadata/dig-linux-${{ matrix.arch }}.buildinfo.txt' artifact-root/metadata/dig.buildinfo.txt

      - name: Upload dig artifact
        uses: actions/upload-artifact@v7
        with:
          name: dig-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  build-socat:
    name: socat (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out socat submodule
        shell: bash
        run: |
          set -euo pipefail
          for attempt in 1 2 3; do
            if git submodule update --init --recursive upstream/socat; then
              exit 0
            fi
            echo "socat submodule checkout attempt $attempt failed; retrying." >&2
            sleep $((attempt * 10))
          done
          exit 1

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build socat
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}' scripts/build-socat-alpine.sh

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata
          cp 'dist/bin/socat-linux-${{ matrix.arch }}' artifact-root/bin/socat
          cp 'dist/metadata/socat-linux-${{ matrix.arch }}.buildinfo.txt' artifact-root/metadata/socat.buildinfo.txt

      - name: Upload socat artifact
        uses: actions/upload-artifact@v7
        with:
          name: socat-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  build-openssl:
    name: openssl (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out OpenSSL submodule
        run: git submodule update --init --recursive upstream/openssl

      - name: Fetch OpenSSL tags
        run: git -C upstream/openssl fetch --tags --force origin

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build OpenSSL
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}' scripts/build-openssl-alpine.sh

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata
          cp 'dist/bin/openssl-linux-${{ matrix.arch }}' artifact-root/bin/openssl
          cp 'dist/metadata/openssl-linux-${{ matrix.arch }}.buildinfo.txt' artifact-root/metadata/openssl.buildinfo.txt

      - name: Upload OpenSSL artifact
        uses: actions/upload-artifact@v7
        with:
          name: openssl-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  build-curl:
    name: curl (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out curl submodule
        run: git submodule update --init --recursive upstream/curl

      - name: Fetch curl tags
        run: git -C upstream/curl fetch --tags --force origin

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build curl
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}' scripts/build-curl-alpine.sh

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata
          cp 'dist/bin/curl-linux-${{ matrix.arch }}' artifact-root/bin/curl
          cp 'dist/metadata/curl-linux-${{ matrix.arch }}.buildinfo.txt' artifact-root/metadata/curl.buildinfo.txt

      - name: Upload curl artifact
        uses: actions/upload-artifact@v7
        with:
          name: curl-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  build-jq:
    name: jq (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out jq submodules
        run: git submodule update --init --recursive upstream/jq

      - name: Fetch jq tags
        run: git -C upstream/jq fetch --tags --force origin

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build jq
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}' scripts/build-jq-alpine.sh

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata
          cp 'dist/bin/jq-linux-${{ matrix.arch }}' artifact-root/bin/jq
          cp 'dist/metadata/jq-linux-${{ matrix.arch }}.buildinfo.txt' artifact-root/metadata/jq.buildinfo.txt

      - name: Upload jq artifact
        uses: actions/upload-artifact@v7
        with:
          name: jq-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  build-nmap:
    name: nmap (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
            qemu: false
          - arch: aarch64
            runner: ubuntu-24.04-arm
            qemu: false
          - arch: armv7
            runner: ubuntu-24.04
            qemu: true
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check out nmap submodule
        run: git submodule update --init --recursive upstream/nmap

      - name: Set up QEMU
        if: ${{ matrix.qemu }}
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm

      - name: Build nmap
        env:
          ALPINE_VERSION: '3.20'
          CFLAGS: -O3 -pipe
        run: ./scripts/ci-build-in-docker.sh '${{ matrix.arch }}' scripts/build-nmap-alpine.sh

      - name: Stage platform artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact-root/bin artifact-root/metadata artifact-root/share
          cp 'dist/bin/nmap-linux-${{ matrix.arch }}' artifact-root/bin/nmap
          cp 'dist/metadata/nmap-linux-${{ matrix.arch }}.buildinfo.txt' artifact-root/metadata/nmap.buildinfo.txt
          cp -a dist/share/nmap artifact-root/share/nmap

      - name: Upload nmap artifact
        uses: actions/upload-artifact@v7
        with:
          name: nmap-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: artifact-root

  package:
    name: Package bundle (${{ matrix.arch }})
    runs-on: ubuntu-24.04
    needs:
      - build-tcpdump
      - build-strace
      - build-gdb
      - build-nmap
      - build-jq
      - build-curl
      - build-openssl
      - build-socat
      - build-dig
      - build-mtr
      - build-lsof
      - build-iproute2
      - build-tshark
      - build-ripgrep
    permissions:
      contents: read
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix:
        arch:
          - x86_64
          - aarch64
          - armv7
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Download tcpdump artifact
        uses: actions/download-artifact@v8
        with:
          name: tcpdump-linux-${{ matrix.arch }}
          path: package-root/tcpdump

      - name: Download strace artifact
        uses: actions/download-artifact@v8
        with:
          name: strace-linux-${{ matrix.arch }}
          path: package-root/strace

      - name: Download gdb artifact
        uses: actions/download-artifact@v8
        with:
          name: gdb-linux-${{ matrix.arch }}
          path: package-root/gdb

      - name: Download nmap artifact
        uses: actions/download-artifact@v8
        with:
          name: nmap-linux-${{ matrix.arch }}
          path: package-root/nmap

      - name: Download jq artifact
        uses: actions/download-artifact@v8
        with:
          name: jq-linux-${{ matrix.arch }}
          path: package-root/jq

      - name: Download curl artifact
        uses: actions/download-artifact@v8
        with:
          name: curl-linux-${{ matrix.arch }}
          path: package-root/curl

      - name: Download OpenSSL artifact
        uses: actions/download-artifact@v8
        with:
          name: openssl-linux-${{ matrix.arch }}
          path: package-root/openssl

      - name: Download socat artifact
        uses: actions/download-artifact@v8
        with:
          name: socat-linux-${{ matrix.arch }}
          path: package-root/socat

      - name: Download dig artifact
        uses: actions/download-artifact@v8
        with:
          name: dig-linux-${{ matrix.arch }}
          path: package-root/dig

      - name: Download mtr artifact
        uses: actions/download-artifact@v8
        with:
          name: mtr-linux-${{ matrix.arch }}
          path: package-root/mtr

      - name: Download lsof artifact
        uses: actions/download-artifact@v8
        with:
          name: lsof-linux-${{ matrix.arch }}
          path: package-root/lsof

      - name: Download iproute2 artifact
        uses: actions/download-artifact@v8
        with:
          name: iproute2-linux-${{ matrix.arch }}
          path: package-root/iproute2

      - name: Download tshark artifact
        uses: actions/download-artifact@v8
        with:
          name: tshark-linux-${{ matrix.arch }}
          path: package-root/tshark

      - name: Download ripgrep artifact
        uses: actions/download-artifact@v8
        with:
          name: ripgrep-linux-${{ matrix.arch }}
          path: package-root/rg

      - name: List downloaded artifacts
        run: find package-root -maxdepth 4 -type f -print | sort

      - name: Package platform zip
        id: package
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${GITHUB_REF_TYPE:-}" == "tag" ]]; then
            version="$GITHUB_REF_NAME"
          else
            version="tcpdump-${GITHUB_SHA::12}"
          fi

          ./scripts/package-platform.sh '${{ matrix.arch }}' "$version" package-root dist

      - name: Upload platform bundle artifact
        uses: actions/upload-artifact@v7
        with:
          name: stalibs-linux-${{ matrix.arch }}
          if-no-files-found: error
          path: ${{ steps.package.outputs.zip_path }}

      - name: Attest platform bundle provenance
        if: github.event_name != 'pull_request'
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: ${{ steps.package.outputs.zip_path }}

  publish-release:
    name: Publish GitHub Release assets
    runs-on: ubuntu-24.04
    needs:
      - package
    if: github.ref_type == 'tag'
    permissions:
      contents: write
    steps:
      - name: Check out source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Fetch upstream tags for release notes
        run: |
          git -C upstream/gdb fetch --tags --force origin
          git -C upstream/jq fetch --tags --force origin
          git -C upstream/curl fetch --tags --force origin
          git -C upstream/openssl fetch --tags --force origin
          git -C upstream/bind9 fetch --tags --force origin
          git -C upstream/lmdb fetch --tags --force origin
          git -C upstream/mtr fetch --tags --force origin
          git -C upstream/lsof fetch --tags --force origin
          git -C upstream/iproute2 fetch --tags --force origin
          git -C upstream/wireshark fetch --tags --force origin
          git -C upstream/ripgrep fetch --tags --force origin

      - name: Download platform bundle artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: stalibs-linux-*
          path: release-assets
          merge-multiple: true

      - name: Publish GitHub Release assets
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        shell: bash
        run: |
          set -euo pipefail

          ./scripts/write-release-notes.sh "$TAG" > release-notes.md

          mapfile -t zip_files < <(find release-assets -maxdepth 1 -type f -name '*.zip' | sort)
          if [[ "${#zip_files[@]}" -eq 0 ]]; then
            echo "No platform zip assets found." >&2
            exit 1
          fi

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "${zip_files[@]}" --clobber
          else
            gh release create "$TAG" "${zip_files[@]}" \
              --title "StaLiBs $TAG" \
              --notes-file release-notes.md
          fi

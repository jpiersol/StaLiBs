# tcpdump

StaLiBs currently builds tcpdump from the pinned submodules in:

- `upstream/tcpdump`
- `upstream/libpcap`

Release tags in this repository do not need to match upstream tcpdump tags; releases use the pinned submodule commits at the pushed Git tag.
